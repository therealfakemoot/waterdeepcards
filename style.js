// Colors
if (card_has_tag(card, "spell")) {
        card.color = "DarkBlue";
}

if (card_has_tag(card, "feature")) {
        card.color = "indigo";
}

if (card_has_tag(card, "equipment")) {
        card.color = "FireBrick";
}

// Icons

if (card_has_tag(card, "soul knife")) {
        card.icon = "abstract-065";
}

if (card_has_tag(card, "trickster")) {
        card.icon = "abstract-003";
}

if (card_has_tag(card, "equipment")) {
        card.icon = "trade";
}
